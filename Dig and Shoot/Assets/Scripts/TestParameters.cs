using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestParameters : MonoBehaviour
{
    [SerializeField] int damageLevel;
    [SerializeField] int fireRateLevel;
    [SerializeField] int baseLevel;
    [SerializeField] int fastShootLevel;
    [SerializeField] int ghostLevel;
    [SerializeField] int frostLevel;
    [SerializeField] int barrelLevel;

    /*private void Awake()
    {
        PlayerPrefs.SetInt("damage", damageLevel);
        PlayerPrefs.SetInt("firerate", fireRateLevel);
        PlayerPrefs.SetInt("basehealth", baseLevel);
        PlayerPrefs.SetInt("fastshootability", fastShootLevel);
        PlayerPrefs.SetInt("ghost", ghostLevel);
        PlayerPrefs.SetInt("frost", frostLevel);
        PlayerPrefs.SetInt("burst", barrelLevel);
    }*/


}
